# Copy this to .env if needed
# Use SQLite by default; no value needed for local development.
# For PostgreSQL, uncomment and set a valid connection string:
# DATABASE_URL=postgresql://username:password@localhost:5432/expense_tracker
